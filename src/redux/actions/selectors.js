export const users = state => state.users.usersInfo

export const currentPage = state => state.users.currentPage

export const totalUsers = state => state.users.totalUsers

export const countUsers = state => state.users.countUsers

export const loader = state => state.users.isLoader

export const disabled = state => state.users.isDisabled